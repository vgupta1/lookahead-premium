# Pinned to the environment the published numbers were produced in
# (Python 3.10.12, Ubuntu 22.04 / glibc 2.35).  Any recent version of these
# packages reproduces the tables; the portfolio premium is solved with CLARABEL,
# which ships with cvxpy, and its values are reproducible to about 1e-7 across
# solver builds rather than bit for bit.
numpy==2.2.6
pandas==2.3.3
matplotlib==3.10.9
cvxpy==1.7.5
scikit-learn==1.7.2

# testing only
pytest>=8.0
